#!/usr/bin/env perl
use strict;
use warnings;
our $VERSION = '1.00';

# this program reads a line of input, and writes the line back out
# read a line of input
my $inputline = <>;

# write the line out
print $inputline;

