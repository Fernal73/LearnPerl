#!/usr/bin/env perl
use strict;
use warnings;

# this program reads a line of input, and writes the line back out
# read a line of input
my $inputline = <STDIN>;

# write the line out
print($inputline );

